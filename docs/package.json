{
  "name": "ccproxy-docs",
  "version": "1.0.0",
  "description": "Documentation for CCProxy",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "../scripts/deploy-cloudflare.sh --production",
    "deploy:preview": "../scripts/deploy-cloudflare.sh --preview",
    "deploy:local": "../scripts/deploy-local.sh"
  },
  "devDependencies": {
    "vitepress": "^0.1.1"
  },
  "type": "module",
  "dependencies": {
    "@algolia/client-search": "^5.33.0",
    "local-search": "^1.1.2",
    "vitepress-plugin-search": "^0.0.3"
  }
}
